// lib/src/models/rive_animation_type.dart

/// Enum to define the type of Rive animation playback
enum RiveAnimationType {
  /// Single animation that plays once and completes
  oneShot,

  /// State machine driven by inputs and transitions
  stateMachine,
}
